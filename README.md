docker compose up -d --build - do uruchomienia całości

localhost:8080 - dozzle do monitoringu

api.test - trzeba dodać w /etc/hosts dla linuxa lub ekwiwalent dla windowsa żeby ładnie działał virtual host
